<img width="1004" height="220" alt="image" src="https://github.com/user-attachments/assets/54fb1e57-8892-443e-a4db-8ed488113335" />
<img width="983" height="91" alt="image" src="https://github.com/user-attachments/assets/e8d90127-1615-43fc-816b-90034fa338c2" /> 
<img width="1814" height="1109" alt="image" src="https://github.com/user-attachments/assets/17822b1b-6adf-48cd-a20d-3e490d508da0" />

Алгоритм: Введення елементів матриці 4×4, ініціалізація змінних sum = 0, count = 0. Для кожного елемента матриці: якщо елемент кратний 5 → додати його до sum, збільшити count, якщо count > 0, обчислити avg = sum / count. Вивести середнє арифметичне
#include <iostream>
#include <windows.h>
using namespace std;

// Функція для обчислення середнього арифметичного елементів, кратних 5
double averageMultiplesOfFive(int matrix[4][4]) {
    int sum = 0;
    int count = 0;

    // Перебір усіх елементів матриці
    for (int i = 0; i < 4; i++) {
        for (int j = 0; j < 4; j++) {
            if (matrix[i][j] % 5 == 0) {
                sum += matrix[i][j];
                count++;
            }
        }
    }

    // Якщо знайдено кратні 5 — повертаємо середнє
    if (count > 0)
        return static_cast<double>(sum) / count;
    else
        return 0; // Якщо таких елементів немає
}

int main() {
    SetConsoleCP(1251);
    SetConsoleOutputCP(1251);
    int matrix[4][4];

    cout << "Введіть елементи матриці 4x4:\n";
    for (int i = 0; i < 4; i++) {
        for (int j = 0; j < 4; j++) {
            cout << "matrix[" << i << "][" << j << "] = ";
            cin >> matrix[i][j];
        }
    }

    double avg = averageMultiplesOfFive(matrix);

    if (avg != 0)
        cout << "\nСереднє арифметичне елементів, кратних 5: " << avg << endl;
    else
        cout << "\nУ матриці немає елементів, кратних 5." << endl;

    return 0;
}


Приклад виконання
10 7 25 4|
5 12 15 8|
9 30 11 2|
20 3 1 6|

Середнє арифметичне елементів, кратних 5: 17.5

### 🧠 Теоретичні знання
[+] Знаю, що таке функція та її основні елементи (назва, параметри, тип, тіло).  
[+] Розумію різницю між оголошенням і визначенням функції.  
[+] Знаю, що таке параметри функції та як вони передаються.  
[+] Можу пояснити, як функція повертає значення оператором `return`.  
---

### 🧩 Практичні навички
[+] У коді є хоча б одна користувацька функція.  
[+] Функція приймає параметри та повертає результат.  
[+] Програма працює без помилок і має приклад виконання.  
[+] Код містить коментарі та оформлений читабельно.  
[+] Додано блок-схему алгоритму.  
---

### 🧰 Робота з GitHub / Gist
[+] Є всі необхідні файли: `main.cpp`, `README.md`, `checklist.md`.  
[+] `README.md` містить завдання, опис алгоритму, приклад виконання, блок-схему та висновок.  
[+] Репозиторій або Gist має зрозумілу назву (наприклад, `lab-functions`) і чітку структуру.  
---

### 🧩 Самооцінка
[+] Можу пояснити, як саме працює моя функція.  
[+] Розумію, у яких задачах доцільно використовувати функції.  
[+] Знаю, як можна вдосконалити свою програму (додати ще функції, параметри тощо).  
[+] Готовий(а) продемонструвати роботу програми під час захисту.
